from django.apps import AppConfig


class CoreConfig(AppConfig):
    name = 'core'
    verbose_name = "Bairros e Categorias -  As informações(Bairros e Categorias) criadas e alteradas refletem diretamente na busca do aplicativo, ou seja, itens criados aqui vão aparecer na tela de busca do Aplicativo."
